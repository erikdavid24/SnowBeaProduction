<div class="gestion-toolbar" style="margin-bottom:8px;">
    <button class="btn btn-primary btn-sm" id="btnAgregarEnsamble"><i class="fas fa-plus"></i> Agregar Ensamble</button>
</div>

@(Html.Kendo().Grid<SnowTrolleyProduction.Models.EnsambleGridRow>()
    .Name("gridEnsambles")
    .HtmlAttributes(new { style = "height: 600px;" })
    .ToolBar(t => t.Search())
    .Columns(cols => {
        cols.Bound(e => e.Numero).Title("Ensamble");
        cols.Bound(e => e.Linea).Title("Linea");
        cols.Bound(e => e.Id).Title("Acciones").Width(120)
            .Sortable(false).Filterable(false)
            .HtmlAttributes(new { style = "white-space:nowrap;" })
            .ClientTemplate(
                "<button class='btn btn-icon btn-sm btn-warning btn-edit-ensamble' data-id='#=Id#' title='Editar'>" +
                "<i class='fas fa-edit'></i></button> " +
                "<button class='btn btn-icon btn-sm btn-danger btn-del-ensamble' data-id='#=Id#' title='Eliminar'>" +
                "<i class='fas fa-trash-alt'></i></button>");
    })
    .Sortable()
    .Filterable()
    .Resizable(r => r.Columns(true))
    .Reorderable(r => r.Columns(true))
    .ColumnMenu()
    .Scrollable(s => s.Height(550))
    .DataSource(ds => ds.Ajax()
        .Read(r => r.Action("EnsamblesJson", "TrolleyGestion"))
        .ServerOperation(false))
)
